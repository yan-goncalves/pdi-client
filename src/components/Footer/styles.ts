import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  container: {
    paddingBottom: 15,
    alignSelf: 'flex-end',
    display: 'flex',
    justifyContent: 'center',
    width: '100%',
    position: 'absolute',
    margin: '0 auto',
    bottom: 0
  }
}))
